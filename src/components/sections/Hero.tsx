export default function Hero() {
  return (
    <section
      id="hero"
      className="min-h-screen flex items-center justify-center"
    >
      <div className="container">
        <h1 className="font-display text-4xl md:text-6xl text-foreground-base text-center transition-colors">
          Hero Section
        </h1>
      </div>
    </section>
  );
}
